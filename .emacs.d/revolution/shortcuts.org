#+BEGIN_COMMENT
## Last update Sat Jan 31 13:27:59 2026 @author Glider
## ======================================================================
## @license Copyright 2016-2025 Pierre Schebath
## ---------------------
## 
## @brief This file has been written by Pierre Etienne Charles Schebath Cazoulat.
## 
## This source code, its related data and algorithms are Pierre Schebath
## Proprietary Information and shall be protected in strict confidence by
## the party who receives it.  It shall not be disclosed nor copied nor
## duplicated in whole or in part to any third party without Pierre Schebath
## written prior permission.
## ======================================================================
## shortcuts.org for revolution in ~/.emacs.d/revolution/
## @description: shortcuts
## 
## Started on  Wed Jun  4 11:17:30 2025 @author Glider
## Last update Wed Jun  4 11:17:30 2025 @author Glider
## ======================================================================
#+END_COMMENT

* Header
#+NAME: BOF_shortcuts()
#+BEGIN_SRC emacs-lisp :lexical t

;;; shortcuts.el --- -*- lexical-binding: t; -*-
;;; Commentary:
;;; shortcuts it's about easy life
;;; code:

#+END_SRC

* Unbind Shortcuts
#+NAME: Unbind Shortcuts
#+BEGIN_SRC emacs-lisp :lexical t


;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;;; Unbind Shortcuts
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

(global-unset-key (kbd "<f3>"))
(global-unset-key (kbd "<f4>"))
(global-unset-key (kbd "<f5>"))

#+END_SRC

* Utilities
#+NAME: Utilities
#+BEGIN_SRC emacs-lisp :lexical t

;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;;;; Utilities
;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;

;; Annuler/Refaire
(global-set-key [(control z)] 'undo)

;;******     Insert an header      ******;;
(global-set-key (kbd "C-c h") 'std-file-header)

;;******  Affiche le mode majeur   ******;;
(global-set-key (kbd "C-c m") 'show-major-mode)

  ;; Documentation du langage
(global-set-key (kbd "<f2>") #'er-describe-symbol-at-point)

;; debuger deplacer depuis f4 en f5 pour une logique d'utilisation
(let ((cmd (lookup-key global-map (kbd "<f4>"))))
  (when (commandp cmd)
    (global-unset-key (kbd "<f4>"))
    (global-set-key (kbd "<f5>") cmd)))

;;(global-set-key (kbd "<f3>") #'projectile-help-command)
(global-set-key (kbd "<f4>") #'which-key-show-top-level)

#+END_SRC

* Shortcut Initialisation
#+NAME: Shortcuts
#+BEGIN_SRC emacs-lisp :lexical t

(use-package which-key
  :ensure t
  :custom
  (which-key-idle-delay 0.2)
  (which-key-idle-secondary-delay 0.2)
  (which-key-separator " → ")
  (which-key-prefix-prefix "+")
  (which-key-sort-order 'which-key-local-then-key-order)
  (which-key-show-docstrings t)
  (which-key-side-window-max-width 0.45)
  :init
  (setq which-key-show-next-page-cycle t)
  (setq which-key-popup-type 'side-window)
  (setq which-key-max-description-length 120)
  (setq which-key-max-display-columns 1)
  (setq which-key-show-prefix 'left)
  (setq which-key-sort-order 'which-key-key-order)
  (which-key-mode))

(which-key-mode 1)
(which-key-setup-side-window-right-bottom)

(defun X-list-side-windows (&optional frame)
  "Retourne la liste des side windows actives."
  (seq-filter
   (lambda (win)
     (window-parameter win 'window-side))
   (window-list frame 'no-minibuffer)))


(defun go-side-window (window width)
  "Resize width of any side window."
  (unless (stringp window)
    (error "WINDOW must be a string"))
  (unless (integerp width)
    (error "WIDTH must be an integer"))
  (dolist (win (X-list-side-windows))
    (when (string-match window (buffer-name (window-buffer win)))
      (let ((delta (- width (window-width win))))
	(with-selected-window win
	  (goto-char (point-min))
	  (forward-line 2))))))

(go-side-window) ;; TODO relier a un hook (affichage which-key) 
;; (global-unset-key (kbd "C-h"))
;; (global-unset-key (kbd "TAB"))
;; (define-key which-key-mode-map (kbd "TAB") #'which-key-C-h-dispatch)

;;(setq which-key-compute-remaps t)

#+END_SRC


#+NAME: EOF_shortcuts()
#+BEGIN_SRC emacs-lisp

(provide 'shortcuts)
 ;;; shortcuts.el ends here

#+END_SRC

